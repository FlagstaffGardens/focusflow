# Runtime configuration for FocusFlow (Next.js + Node)
PORT=3000
DATA_DIR=./data          # Use /data when running inside Docker/Dokploy

# Optional: transcription via AssemblyAI
ASSEMBLYAI_API_KEY=

# Required for summarization
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-4

# Optional overrides
# PROMPT_PATH=prompts/meeting_summary.md
